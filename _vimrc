" Impostazione dell'encoding
set encoding=utf-8

" Attivazione dell'evidenziazione della sintassi
syntax on

" Impostazioni della lingua
language en

" Numero ad inizio schermo per il comando Z Enter
set scrolloff=5

" Keep 1000 items in ithe history
set history=1000

" Show the cursor position
set ruler

" Show incomplete commands in the bottom right window corner
set showcmd

" Show a menu when using tab completion
set wildmenu

" Numero di righe da mantenere quando si usa Z enter
set scrolloff=5

" Disabilita l'evidenziazione dei risultati di una ricerca
set nohlsearch

" Evidenza la parola trovata sul quale si trova il cursore
set incsearch

" Permette di fare ricerche case insensitive
set ignorecase

" Permette di fare ricerche case sensitive quando si usano caratteri maiuscoli disabilitando ingnorecase
set smartcase

" Attiva la numerazione delle righe
set number

" Wraping del file di testo senza dividere le parole
set lbr

" Attiva indentazione automatica
set ai

" Attiva indentazione intelligente
set si

" Attiva un background scuro
set bg=dark

" Selezione schema colore
color default 

" Remap del tasto Esc
inoremap jj <Esc>
